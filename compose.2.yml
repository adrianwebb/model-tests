services:
  qwen-flash-next:
    image: ghcr.io/ggml-org/llama.cpp:server-cuda
    container_name: qwen-flash-next
    restart: unless-stopped

    ports:
      - "8080:8080"

    volumes:
      - ./models:/models

    environment:
      LLAMA_CACHE: /models/cache

      LLAMA_ARG_HF_REPO: "AtomicChat/Qwen3.8-Flash-Next-GGUF:Q4_K_M"

      LLAMA_ARG_HOST: "0.0.0.0"
      LLAMA_ARG_PORT: "8080"
      LLAMA_ARG_ALIAS: "qwen3.8-flash-next"

      LLAMA_ARG_CTX_SIZE: "8192"

      # Test 8 concurrent slots
      LLAMA_ARG_N_PARALLEL: "8"

      LLAMA_ARG_THREADS: "48"

      LLAMA_ARG_CPU_MOE: "1"

      LLAMA_ARG_N_GPU_LAYERS: "auto"

      LLAMA_ARG_OVERRIDE_TENSOR: "per_layer_token_embd=CPU"

      # Automatic fitting cannot operate with our tensor overrides anyway.
      LLAMA_ARG_FIT: "on"
      LLAMA_ARG_FIT_TARGET: "1536"

      LLAMA_ARG_CACHE_TYPE_K: "q8_0"
      LLAMA_ARG_CACHE_TYPE_V: "q8_0"

      LLAMA_ARG_FLASH_ATTN: "auto"

      # TEXT-ONLY TEST:
      # Do not load Qwen's ~865 MiB vision/mmproj onto the GPU.
      LLAMA_ARG_MMPROJ_AUTO: "false"

      LLAMA_ARG_ENDPOINT_METRICS: "1"
      LLAMA_ARG_PERF: "1"

    gpus: all